const INITIAL_STATE = {
  name: '',
  assertions: 0,
  score: 0,
  gravatarEmail: '',
};

export const userInfo = (state = INITIAL_STATE, action) => {
  switch (action.type) {
  default:
    return state;
  }
};
